const kNavigationDuration = Duration(milliseconds: 300);
const kToastAnimationDuration = Duration(milliseconds: 1200);
const kToastCloseDuration = Duration(seconds: 3);
const kMovieCardDuration = Duration(milliseconds: 600);
